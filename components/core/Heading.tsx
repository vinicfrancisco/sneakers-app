import { Text as RNText } from 'react-native'
import { styled } from '@gluestack-style/react'

const Heading = styled(RNText, {
  color: '$black',
  fontFamily: 'NunitoBold',
  fs: '$large',

  variants: {},
})

export default Heading
